// Placeholder user profile.
//
// There is no profile / settings UI yet. When one is built, this is the ONLY
// place to change: set `userName` to the stored value. Anything that greets
// the user by name reads it from here.

/** The user's display name, or null until they set one. */
export const userName: string | null = null;
